# 服务相关
server:
  port: 8081
  name: ivay-manage

# 数据库配置
dataSource:
  master: true
  slave: true


spring:
  profiles:
    # 多模块使用include取代active
    include: dev, comDev, dbDev
#    include: test, comTest, dbTest
#    include: prod, comProd, dbProd

# 增加定时器
timer:
  autoAudit: 0 */10 * * * ?
